# Supabase (P8+). Both values are publishable — they ship in the client bundle;
# row-level security guards the data. The app has working fallbacks for the
# production project baked into src/lib/supabaseClient.js, so these are only
# needed to point a build at a DIFFERENT Supabase project.
VITE_SUPABASE_URL=https://qmgslxaladefllaazdex.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_dFsF0HgsU8SCuNaDejI9Bw_tTVf6P75
